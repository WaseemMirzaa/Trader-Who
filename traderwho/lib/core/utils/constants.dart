/// This file contains all the constants used in the app
///
/// [kIsLoggedIn] is used as a key to store a boolean indicating whether the user is currently logged in.
const String kIsLoggedIn = 'user_already_logged_in';

/// [kUsername] is used as a key to store the username of the user.
const String kUsername = 'username';

/// [kPassword] is used as a key to store the password of the user.
const String kPassword = 'user_password';

///[kSelectedLanguage] is used as a key to store the selected language of the user.
const String kSelectedLanguage = 'user_selected_language';

///[userData] is used as a key to store the user data of the user.
const String userData = 'user_data';

///[rememberMe] is used as a key to store a boolean indicating whether the user wants to be remembered.
const String rememberMe = 'remember_me';

///[rememberUser] is used as a key to store a boolean indicating whether the user wants to be remembered.
const String rememberUser = 'remember_user';

///[rememberPassword] is used as a key to store a boolean indicating whether the user wants to be remembered.
const String rememberPassword = 'remember_password';

///[accessToken] is used as a key to store the access token of the user.
const String accessToken = 'access_token';

///[refreshToken] is used as a key to store the refresh token of the user.
const String refreshToken = 'refresh_token';
